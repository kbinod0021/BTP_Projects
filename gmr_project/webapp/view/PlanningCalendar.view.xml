<mvc:View
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:layout="sap.ui.layout"
    xmlns:core="sap.ui.core"
    controllerName="gmrproject.controller.PlanningCalendar"
xmlns:unified="sap.ui.unified">
  
  
<VBox class="sapUiSmallMargin">

    <Button text="Add Appointment" press="onOpenDialog" />

 <PlanningCalendar
    id="PC1"
    startDate="{/startDate}"
    viewKey="Hour"
    rows="{/people}"
    appointmentsVisualization="Standard"
    showEmptyIntervalHeaders="false"
    showWeekNumbers="true">

    <rows>
        <PlanningCalendarRow 
            title="{name}"
            key="{key}"
           	text="{role}"
					appointments="{path : 'appointments', templateShareable: false}"
					intervalHeaders="{path: 'headers', templateShareable: false}">

            <appointments>
                <unified:CalendarAppointment
                   	startDate="{start}"
							endDate="{end}"
							icon="{pic}"
							title="{title}"
							text="{info}"
							type="{type}" />
            </appointments>
	<intervalHeaders>
						<unified:CalendarAppointment
							startDate="{start}"
							endDate="{end}"
							icon="{pic}"
							title="{title}"
							type="{type}">
						</unified:CalendarAppointment>
					</intervalHeaders>
        </PlanningCalendarRow>
    </rows>
</PlanningCalendar>


  </VBox>


</mvc:View>
